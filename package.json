{
  "name": "q-party",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start-dev": "NODE_ENV='developmegint' npm run build-client-watch & NODE_ENV='development' npm run start-server",
    "build-client-watch": "webpack -w",
    "start-server": "nodemon server -e html,js,scss --ignore public --ignore client",
    "start": "node server.js",
    "deploy": "script/deploy",
    "heroku-token": "script/encrypt-heroku-auth-token.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "phaser": "^3.22.0",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "raw-loader": "^0.5.1",
    "webpack": "^3.4.1"
  }
}
